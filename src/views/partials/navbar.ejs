<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <div class="d-flex justify-content-between align-items-center w-100">
            <div class="navbar-title text-start">
                <% if (active === 'upload') { %>
                    <h1 class="h4 mb-0">Analizador de Descuadres</h1>
                <% } else if (active === 'analysis') { %>
                    <h1 class="h4 mb-0">Análisis de Descuadres</h1>
                <% } else if (active === 'reports') { %>
                    <h1 class="h4 mb-0">Reportes de Descuadres</h1>
                <% } else if (active === 'managed') { %>
                    <h1 class="h4 mb-0">Gestión de Descuadres</h1>
                <% } else if (active === 'mismatches') { %>
                    <h1 class="h4 mb-0">Histórico de Descuadres</h1>
                <% } %>
            </div>
            <% if (user) { %>
                <div class="dropdown">
                    <button class="btn btn-link dropdown-toggle nav-user-button" 
                            type="button" 
                            id="userDropdown" 
                            data-bs-toggle="dropdown" 
                            aria-expanded="false">
                        <div class="d-flex align-items-center">
                            <div class="user-avatar">
                                <i class="bi bi-person"></i>
                            </div>
                            <span class="user-name"><%= user.nombre %></span>
                        </div>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                        <li>
                            <span class="dropdown-item-text">
                                <i class="bi bi-person-badge"></i>
                                <%= user.username %>
                            </span>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item text-danger" href="/auth/logout">
                                <i class="bi bi-box-arrow-right"></i>
                                Cerrar sesión
                            </a>
                        </li>
                    </ul>
                </div>
            <% } %>
        </div>
    </div>
</nav>

<div class="sidebar">
    <div class="sidebar-header">
        <a class="navbar-brand" href="/upload">
            <img src="/public/img/logo.png" alt="APD Logo" class="sidebar-logo" />
        </a>
    </div>
    
    <ul class="nav flex-column">
        <li class="nav-item">
            <a class="nav-link <%= active === 'upload' ? 'active' : '' %>" href="/upload">
                <i class="bi bi-cloud-upload me-2"></i> Subir
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <%= active === 'mismatches' ? 'active' : '' %>" href="/mismatches">
                <i class="bi bi-table me-2"></i> Histórico
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <%= active === 'reports' ? 'active' : '' %>" href="/reports">
                <i class="bi bi-graph-up-arrow me-2"></i> Reportes
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <%= active === 'analysis' ? 'active' : '' %>" href="/analysis">
                <i class="bi bi-clipboard2-data me-2"></i> Análisis
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <%= active === 'managed' ? 'active' : '' %>" href="/managed">
                <i class="bi bi-check2-circle me-2"></i> Gestionar
            </a>
        </li>
    </ul>

    <div class="sidebar-footer">
        <% if (user) { %>
            <div class="user-section">
                <a href="/auth/logout" class="btn btn-link logout-btn">
                    <i class="bi bi-box-arrow-right"></i>
                    Cerrar sesión
                </a>
                <div class="user-info">
                  <div class="user-avatar">
                      <i class="bi bi-person"></i>
                  </div>
                  <span class="user-name"><%= user.nombre %></span>
              </div>
            </div>
        <% } %>
    </div>
</div>

<div class="content-wrapper">